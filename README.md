# React Elementis

A development are kit for React components.

## Features

- Use `vite` as a bundler.

## Installation

1. Clone repository into your machine.
2. Run:
   ```shell
   cd react-elementis
   npm install
   npm run dev
   ```
